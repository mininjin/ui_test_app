/** @type {import('@vue/cli-service').ProjectOptions} */

const config = {
  publicPath:"./",
  pages: {
    index: {
      title: "UI test app",
      entry: "src/main.ts",
    },
  },
};

module.exports = config;
